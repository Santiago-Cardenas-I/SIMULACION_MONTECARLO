[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "simulacion_montecarlo"
version = "0.1.0"
description = "Librería para simulación estadística y muestreo avanzado"
dependencies = [
    "numpy",
    "scipy",
    "matplotlib"
] 